html
  head 
    title Users 
  body
    include ./header.pug     
    h1 Here are the Users
    each user in users
      li
        a(href=`/user/${user.username}`)= user.username
        br